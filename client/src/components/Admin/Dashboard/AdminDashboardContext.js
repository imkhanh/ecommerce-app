export const adminDashboardState = {};

export const adminDashboardReducer = (state = adminDashboardState, action) => {
	switch (action.type) {
		case '':
			return state;
		default:
			return state;
	}
};
